apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  # The kustomization selects the targets. Kustomize ignores this name.
  name: protect-crds
  annotations:
    argocd.argoproj.io/sync-options: Prune=false
